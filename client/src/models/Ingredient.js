import SharedIngredient from '../../../shared/models/Ingredient';

export default class Ingredient extends SharedIngredient {
  // For now this class just exists to make the Ingredient model easy to import in the front end
}